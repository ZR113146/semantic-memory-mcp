# Reusable: smoke test every binary (standard + UI, all platforms)
name: Smoke

on:
  workflow_call: {}

permissions:
  contents: read

jobs:
  smoke-unix:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
          - os: ubuntu-24.04-arm
            goos: linux
            goarch: arm64
          - os: macos-14
            goos: darwin
            goarch: arm64
          - os: macos-15-intel
            goos: darwin
            goarch: amd64
        variant: [standard, ui]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: binaries-${{ matrix.goos }}-${{ matrix.goarch }}

      - name: Extract binary
        run: |
          SUFFIX=${{ matrix.variant == 'ui' && '-ui' || '' }}
          tar -xzf semantic-memory-mcp${SUFFIX}-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz
          chmod +x semantic-memory-mcp

      - name: Start artifact server
        run: |
          mkdir -p /tmp/smoke-server
          cp semantic-memory-mcp /tmp/smoke-server/
          OS=${{ matrix.goos }}; ARCH=${{ matrix.goarch }}
          SUFFIX=${{ matrix.variant == 'ui' && '-ui' || '' }}
          tar -czf "/tmp/smoke-server/semantic-memory-mcp${SUFFIX}-${OS}-${ARCH}.tar.gz" \
            -C /tmp/smoke-server semantic-memory-mcp
          if [ -n "$SUFFIX" ]; then
            cp "/tmp/smoke-server/semantic-memory-mcp${SUFFIX}-${OS}-${ARCH}.tar.gz" \
               "/tmp/smoke-server/semantic-memory-mcp-${OS}-${ARCH}.tar.gz"
          fi
          cd /tmp/smoke-server
          sha256sum *.tar.gz > checksums.txt 2>/dev/null || shasum -a 256 *.tar.gz > checksums.txt
          python3 -m http.server 18080 -d /tmp/smoke-server &

      - name: Smoke test
        run: scripts/smoke-test.sh ./semantic-memory-mcp
        env:
          SMOKE_DOWNLOAD_URL: http://localhost:18080

      - name: Security audits
        run: |
          scripts/security-strings.sh ./semantic-memory-mcp
          scripts/security-install.sh ./semantic-memory-mcp
          scripts/security-network.sh ./semantic-memory-mcp

      - name: MCP robustness test (linux-amd64 standard only)
        if: matrix.variant == 'standard' && matrix.goos == 'linux' && matrix.goarch == 'amd64'
        run: |
          scripts/security-fuzz.sh ./semantic-memory-mcp
          scripts/security-fuzz-random.sh ./semantic-memory-mcp 60

      - name: ClamAV scan (Linux)
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq clamav > /dev/null 2>&1
          sudo sed -i 's/^Example/#Example/' /etc/clamav/freshclam.conf 2>/dev/null || true
          grep -q "DatabaseMirror" /etc/clamav/freshclam.conf 2>/dev/null || \
            echo "DatabaseMirror database.clamav.net" | sudo tee -a /etc/clamav/freshclam.conf > /dev/null
          sudo freshclam --quiet
          clamscan --no-summary ./semantic-memory-mcp

      - name: ClamAV scan (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          brew install clamav > /dev/null 2>&1
          CLAMAV_ETC=$(brew --prefix)/etc/clamav
          if [ ! -f "$CLAMAV_ETC/freshclam.conf" ]; then
            cp "$CLAMAV_ETC/freshclam.conf.sample" "$CLAMAV_ETC/freshclam.conf" 2>/dev/null || true
            sed -i '' 's/^Example/#Example/' "$CLAMAV_ETC/freshclam.conf" 2>/dev/null || true
            echo "DatabaseMirror database.clamav.net" >> "$CLAMAV_ETC/freshclam.conf"
          fi
          freshclam --quiet --no-warnings 2>/dev/null || freshclam --quiet 2>/dev/null || echo "WARNING: freshclam update failed"
          clamscan --no-summary ./semantic-memory-mcp

  smoke-windows:
    strategy:
      fail-fast: false
      matrix:
        variant: [standard, ui]
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - uses: msys2/setup-msys2@66cd2cce69caa17b53920067426061ca1de3a884 # v2
        with:
          msystem: CLANG64
          path-type: inherit
          install: >-
            mingw-w64-clang-x86_64-python3
            unzip
            zip
            coreutils

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: binaries-windows-amd64

      - name: Extract binary
        shell: msys2 {0}
        run: |
          SUFFIX=${{ matrix.variant == 'ui' && '-ui' || '' }}
          unzip -o "semantic-memory-mcp${SUFFIX}-windows-amd64.zip"
          [ -n "$SUFFIX" ] && cp "semantic-memory-mcp${SUFFIX}.exe" semantic-memory-mcp.exe || true

      - name: Start artifact server
        shell: msys2 {0}
        run: |
          mkdir -p /tmp/smoke-server
          cp semantic-memory-mcp.exe /tmp/smoke-server/
          SUFFIX=${{ matrix.variant == 'ui' && '-ui' || '' }}
          cd /tmp/smoke-server
          zip -q "semantic-memory-mcp${SUFFIX}-windows-amd64.zip" semantic-memory-mcp.exe
          if [ -n "$SUFFIX" ]; then
            cp "semantic-memory-mcp${SUFFIX}-windows-amd64.zip" "semantic-memory-mcp-windows-amd64.zip"
          fi
          sha256sum *.zip > checksums.txt
          python3 -m http.server 18080 -d /tmp/smoke-server &

      - name: Smoke test
        shell: msys2 {0}
        run: scripts/smoke-test.sh ./semantic-memory-mcp.exe
        env:
          SMOKE_DOWNLOAD_URL: http://localhost:18080

      - name: Security audits
        shell: msys2 {0}
        run: |
          scripts/security-strings.sh ./semantic-memory-mcp.exe
          scripts/security-install.sh ./semantic-memory-mcp.exe

      - name: Windows Defender scan
        shell: pwsh
        run: |
          & "C:\Program Files\Windows Defender\MpCmdRun.exe" -SignatureUpdate 2>$null
          $result = & "C:\Program Files\Windows Defender\MpCmdRun.exe" -Scan -ScanType 3 -File "$PWD\semantic-memory-mcp.exe" -DisableRemediation
          Write-Host $result
          if ($LASTEXITCODE -ne 0) { Write-Host "BLOCKED: Windows Defender flagged binary!"; exit 1 }
          Write-Host "=== Windows Defender: clean ==="

  smoke-linux-portable:
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
        variant: [standard, ui]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: binaries-linux-${{ matrix.arch }}-portable

      - name: Extract and smoke test
        run: |
          SUFFIX=${{ matrix.variant == 'ui' && '-ui' || '' }}
          tar -xzf semantic-memory-mcp${SUFFIX}-linux-${{ matrix.arch }}-portable.tar.gz
          chmod +x semantic-memory-mcp
          scripts/smoke-test.sh ./semantic-memory-mcp

      # The -portable binary is what all linux install/update paths now deliver;
      # it MUST start on old glibc (Debian 11 / RHEL 8 / Ubuntu 20.04). Runs it
      # in debian:bullseye (glibc 2.31) — the standard dynamic binary would fail
      # here with `GLIBC_2.38 not found`.
      - name: Old-glibc compatibility
        run: scripts/ci/check-glibc-compat.sh ./semantic-memory-mcp

      - name: Security audits
        run: |
          scripts/security-strings.sh ./semantic-memory-mcp
          scripts/security-install.sh ./semantic-memory-mcp
          scripts/security-network.sh ./semantic-memory-mcp

