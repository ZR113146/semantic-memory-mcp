---
schema: "semantic-memory-adr/v1"
authority: "sqlite"
generated: true
id: "itm-58766901192100-101"
kind: "decision"
layer: "adr"
title: "ADR identity: decision/constraint 记忆默认 layer=\"adr\"(而非 episodic)，以便差异化检索/排序/衰减"
summary: "ADR identity: decision/constraint 记忆默认 layer=\"adr\"(而非 episodic)，以便差异化检索/排序/衰减"
entity_key: "adr-layer-identity"
predicate: "decides"
version: 2
status: "active"
supersedes: "itm-704259499620-5"
scope:
  user: null
  project: "D-semantic-memory-mcp"
  task: null
created_at: 1783529364000
about_code:
  - "D-semantic-memory-mcp.src.mcp.mcp.handle_events"
content_sha256: "6572379b0a6c285091349568440c30527e0624225e39e6d7d85127c5b21eb4c8"
---

# ADR identity: decision/constraint 记忆默认 layer="adr"(而非 episodic)，以便差异化检索/排序/衰减

## Summary

ADR identity: decision/constraint 记忆默认 layer="adr"(而非 episodic)，以便差异化检索/排序/衰减

## Content

[Decision] kind=decision 和 kind=constraint 的记忆写入时，若调用方未显式传 layer，默认设 layer="adr"（而非 "episodic"）。落地在 handle_events（src/mcp/mcp.c，P0-a）。

[Context] 截至 2026-07-07，97%（66/68 条）决策记忆以 layer="episodic" 存储。handle_events 硬编码默认 item.layer = layer ? layer : "episodic" 无条件适用所有 kind，导致无法按 layer 过滤、排级或设差异化 decay。schema 中 layer 的 "adr" 值虽定义但从未作默认。

[Rejected alternatives] (a) 改 schema DEFAULT（ALTER TABLE）：需迁移 87 条已有行，风险大且对未传 layer 的调用方可能静默破坏。(b) 独立 ADR 表：分叉写入路径、测试面翻倍、无实质收益——memory_item 已有所需全部列。

[Anchors] 提交 8d742b7(feat(adr): ADR memory system optimization — identity, scoring, indexing，2026-07-07)。handle_events 的 layer 默认(原引 mcp.c ~L5026，现 mcp.c 内 handle_events)；配套 decay 调优原在 store.c，随 38f8d25 迁至 src/memory/memory_store.c。
