---
schema: "semantic-memory-adr/v1"
authority: "sqlite"
generated: true
id: "itm-48676743187900-15"
kind: "decision"
layer: "adr"
title: "写入门的锚定/结构/引用三维为什么以 advice 提示而绝不拒绝写入"
summary: "写入门的锚定/结构/引用三维为什么以 advice 提示而绝不拒绝写入"
entity_key: "write-gate-helper"
predicate: "decides"
version: 2
status: "active"
supersedes: "itm-949922486338-26"
scope:
  user: null
  project: "D-semantic-memory-mcp"
  task: null
created_at: 1783519274000
about_code:
  - "D-semantic-memory-mcp.src.mcp.mcp.handle_events"
  - "D-semantic-memory-mcp.src.mcp.mcp.memory_structure_advice"
content_sha256: "9afc769646b1669da50f25f1fe897e55c3edc85448341d2d2f7cab9676eac577"
---

# 写入门的锚定/结构/引用三维为什么以 advice 提示而绝不拒绝写入

## Summary

写入门的锚定/结构/引用三维为什么以 advice 提示而绝不拒绝写入

## Content

[Decision] 写入门的三个维度(锚定/结构/引用)以 advice 形式提示，绝不拒绝写入。响应字段：anchored(bool)、anchoring_advice(无锚 decision 类)、structure_advice(缺 ADR 四段结构)。结构检测故意宽松(中英关键词、不要求格式、>=80字才查)，只在缺"否决的替代"等要素时轻提示。

[Context] format gate 原来只防畸形(非空/不双编码/summary≠content)。长出三维但定调为 helper 而非 gatekeeper——因为纯 memory 项目必须可写(自动召回 hook 是它唯一不可替代的投递通道)，硬拒会砸掉这个通道和存量记忆。

[Rejected alternatives] 硬强制 about_code(decision 无锚就拒绝)——会让纯 memory 项目无法写入，违背"纯 memory 被容忍、可见、非特权"的定性。

[Anchors] memory_structure_advice(宽松结构检测，现 src/mcp/mcp.c)+ handle_events(响应字段与 advice 组装、supersedes 入参接收，现 src/mcp/mcp.c)；提交 3948ad7(P3-b 锚定维，2026-06-30) + eab6935(P3-d 结构+引用维，2026-06-30)。此两维只动 mcp.c，无 store.c 路径。
